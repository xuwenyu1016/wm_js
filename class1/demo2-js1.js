//var x;
//x = 1;

//alert(x);     // undefined
var x = 1;  // 声明 和 赋值，先声明，再赋值 1是立即数
var y;  // y是变量
y = x + 1; // 这是表达式，等号表达式

var a1 = 3,
    a2 = 4,
    a3 = 5; // 这也都是表达式，逗号表达式

document.write('a1 = ' + a1 + '<br/>' + ' a2 = ' + a2);

//alert(aa);  // 报错
aa = 5;

//在一个变量未定义的时候给他赋值，1：不会报错；2：在他这行定义之前的任何位置引用他，都会报错(is not defined)，之后的地方都可以；

// 变量必须以 字母， $，_ 开头，后面可以接字母，$，数字，_

// 数据类型：原始类型-number, string, boolean, undefined, null; 引用类型- array, object, function


